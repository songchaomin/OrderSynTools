spring:
  profiles:
    active: prod
  application:
    name: OrderSynTools
http:
  encoding:
  charset: utf-8
  force: true
  enabled: true

#设置服务端口
server:
  tomcat:
    uri-encoding: UTF-8
  port: 9000
  servlet:
    context-path: /OrderSynTools

logging:
  config: classpath:logback-spring.xml
  path: /OrderSynToolsLog

reg:
  sn: 8539fd8c9fbce80a202d312485d2b3ae


